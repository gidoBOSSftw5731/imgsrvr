package main

/*
Heres the config, put your stuff in the quotations and everyone will be happy, OK?
Make a copy of this into config.go and check differences in every commit
*/

const (
	urlPrefix  = "/" //Must be atleast a "/" AND end with a "/", unless localhost
	defaultImg = "/home/gido5731/work/imgsrvr/testingpics/Graphic1-50.jpg"
	imgHash    = 6
	imgStore   = "/var/tmp/imgStorage/"
	baseURL    = "https://imagen.click/" //MUST INCLUDE HTTP/HTTPS
)
